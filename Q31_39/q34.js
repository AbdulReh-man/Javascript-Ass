/* Write a JavaScript function that takes two numbers as parameters and returns their sum. */

let cal_sum=(n1, n2)=>
{
    return n1+n2;
}

const num1=54;
const num2=46;
console.log(`The Sum of ${num1} and ${num2} is--> ${cal_sum(num1, num2)}`);